{% extends "SriniBlogBundle::layout.html.twig" %}

{% block title "Hello " ~ name %}

{% block content %}
    <h1>Hello {{ name|upper }}!</h1>
    {% for i in 0..10 %}
        <div class="{{ cycle(['odd', 'even'], i) }}">{{ i }}</div>
    {% endfor %}    
{% endblock %}
